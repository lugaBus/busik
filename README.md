# LugaBus Project

Проект состоит из трех отдельных репозиториев:
- **backend** - Единый API на NestJS для двух сервисов (public и admin)
- **frontend-web** - Публичный фронтенд на NextJS
- **admin-web** - Админский фронтенд на NextJS

## 🛠 Технологический стек

- **Backend**: NestJS, PostgreSQL, Prisma, JWT
- **Frontend**: Next.js 14, TypeScript, React
- **Database**: PostgreSQL
- **Containerization**: Docker, Docker Compose
- **Reverse Proxy**: Nginx

## 📁 Структура проекта

```
project/
├─ backend/                     # ЕДИНЫЙ API
│  ├─ src/
│  │  ├─ modules/
│  │  │  ├─ public/             # 🔓 Public API (frontend)
│  │  │  ├─ admin/              # 🔐 Admin API (admin-web)
│  │  │  └─ shared/             # 🧠 Общая доменная логика
│  │  ├─ auth/                  # JWT, guards, strategies
│  │  ├─ infrastructure/        # DB, cache, mail, queues
│  │  ├─ main.ts
│  │  └─ app.module.ts
│  ├─ prisma/
│  └─ Dockerfile
│
├─ frontend-web/                # 🌍 Public frontend
│  ├─ src/
│  │  ├─ app/
│  │  ├─ api/
│  │  ├─ auth/
│  │  └─ ui/
│  └─ Dockerfile
│
├─ admin-web/                   # 🛠 Admin frontend
│  ├─ src/
│  │  ├─ app/
│  │  ├─ api/
│  │  ├─ auth/
│  │  └─ ui/
│  └─ Dockerfile
│
└─ docker/
   ├─ docker-compose.yml
   ├─ nginx.conf
   └─ env/
```

## 🚀 Быстрый старт

### Предварительные требования

- Node.js 20+
- Docker и Docker Compose
- npm или yarn

### Установка и запуск

1. **Клонируйте репозиторий** (если необходимо)

2. **Настройте переменные окружения**

   Отредактируйте файлы в `docker/env/`:
   - `backend.env` - настройки бэкенда и базы данных
   - `frontend.env` - настройки публичного фронтенда
   - `admin.env` - настройки админского фронтенда

3. **Запустите проект через Docker Compose**

   ```bash
   cd docker
   docker-compose up -d
   ```

   Это запустит:
   - PostgreSQL на порту 5432
   - Backend API на порту 3001
   - Frontend Web на порту 3000
   - Admin Web на порту 3002
   - Nginx на порту 80

4. **Инициализируйте базу данных**

   ```bash
   cd backend
   npm install
   npx prisma generate
   npx prisma migrate dev
   ```

### Локальная разработка (без Docker)

#### Backend

```bash
cd backend
npm install
npx prisma generate
npx prisma migrate dev
npm run start:dev
```

Backend будет доступен на `http://localhost:3001`
Swagger документация: `http://localhost:3001/api/docs`

#### Frontend Web

```bash
cd frontend-web
npm install
npm run dev
```

Доступен на `http://localhost:3000`

#### Admin Web

```bash
cd admin-web
npm install
npm run dev
```

Доступен на `http://localhost:3002`

## 📚 API Документация

После запуска бэкенда, Swagger документация доступна по адресу:
- `http://localhost:3001/api/docs` (при локальной разработке)
- `http://localhost/api/docs` (через Nginx)

## 🔐 Аутентификация

Проект использует JWT для аутентификации. Токены передаются в заголовке `Authorization: Bearer <token>`.

### Роли пользователей

- `user` - обычный пользователь (доступ к public API)
- `admin` - администратор (доступ к admin API)

## 🗄 База данных

Проект использует PostgreSQL с Prisma ORM. Схема базы данных находится в `backend/prisma/schema.prisma`.

### Миграции

```bash
cd backend
npx prisma migrate dev        # Создать новую миграцию
npx prisma migrate deploy     # Применить миграции в production
npx prisma studio            # Открыть Prisma Studio
```

## 🐳 Docker

### Сборка образов

```bash
cd docker
docker-compose build
```

### Просмотр логов

```bash
docker-compose logs -f [service-name]
```

### Остановка

```bash
docker-compose down
```

### Очистка данных

```bash
docker-compose down -v
```

## 📝 Разработка

### Backend модули

- **Public Module** (`/api/public/*`) - публичные эндпоинты для frontend-web
- **Admin Module** (`/api/admin/*`) - защищенные эндпоинты для admin-web
- **Shared Module** - общая бизнес-логика, entities, DTOs, сервисы

### Добавление нового модуля

1. Создайте модуль в соответствующей директории (`public/` или `admin/`)
2. Добавьте контроллеры, сервисы, DTOs
3. Импортируйте модуль в `app.module.ts`

## 🔧 Настройка

### Переменные окружения

Основные переменные окружения находятся в `docker/env/`. Для локальной разработки создайте `.env` файлы в корне каждого проекта.

### CORS

Настройки CORS находятся в `backend/src/main.ts`. Убедитесь, что `FRONTEND_URL` соответствует вашему фронтенду.

## 📦 Скрипты

### Backend

- `npm run start:dev` - запуск в режиме разработки
- `npm run build` - сборка проекта
- `npm run start:prod` - запуск production версии
- `npm run prisma:generate` - генерация Prisma клиента
- `npm run prisma:migrate` - применение миграций

### Frontend

- `npm run dev` - запуск в режиме разработки
- `npm run build` - сборка проекта
- `npm run start` - запуск production версии

## 🤝 Вклад в проект

1. Создайте ветку для новой функции
2. Внесите изменения
3. Создайте Pull Request

## 📄 Лицензия

[Укажите лицензию]

## 👥 Авторы

[Укажите авторов]
